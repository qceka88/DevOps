#start with init to start the configuration
terraform init
# to format configurations
terraform fmf
# to  validate configurations
terraform validate
# to create infrastructure
terraform apply
#inspect current state
terraform show
#to destroy configuration. delete the container and image
terraform destroy